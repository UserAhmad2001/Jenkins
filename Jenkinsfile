pipeline {
  agent any
  stages {
    stage('Retrieving repo') {
      steps {
        sh 'git clone https://github.com/UserAhmad2001/Jenkins.git'
      }
    }

    stage('Going into folder') {
      steps {
        sh '''ls -la
'''
      }
    }

  }
}